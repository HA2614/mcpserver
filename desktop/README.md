# Desktop Launcher

## Install
No extra Python package is required for browser launcher mode.

## Attach mode (backend already running)
`python desktop/launcher.py --url http://127.0.0.1:4000`

## Start-and-host mode
`python desktop/launcher.py --start-backend --backend-cwd . --url http://127.0.0.1:4000`
